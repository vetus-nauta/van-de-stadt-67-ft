# Независимая проверка V02

Дата: 2026-09-10. Проверяющий: `/root/full_model_check`. Статус: **PASS_CACHED_BREP_VISUAL_EXPORT**, только в указанной области.

В трёх переданных ZIP повторяется один и тот же `2-1.3dm` с SHA-256 `814ecb9ae6afa27963f288d52df8db9e67d5c43bf967131b89b5ff40f1c3caa8`. Состав архивов проверен заново, включая исходные ZIP в Downloads; отдельной более подробной 3D-модели среди их файлов нет. Исходный Rhino содержит 491 объект: 314 Brep, 54 LineCurve, 46 NurbsCurve, 31 Text, 20 PolylineCurve, 15 Point, 9 PolyCurve и 2 ArcCurve. Все 516 граней Brep имеют сохранённые render meshes.

Предыдущий внешний вид использовал лишь 8 выбранных Brep. Новый экспорт действительно включает все 314 Brep исходника: 125 026 вершин и 147 591 треугольный/четырёхугольный полигон. Собственный скрипт проверяющего заново прочитал исходник и проверил каждый UUID, каждую вершину, каждый индекс полигона и все границы объектов. Максимальное отклонение при текстовой записи OBJ — 5,01 × 10⁻¹¹ м; границы в metadata совпадают. Проверка использует отдельный код, но тот же движок rhino3dm.

Отдельный скрипт проверяющего открыл сохранённый `.blend` и сверил все 314 UUID, все вершины в мировых координатах и все индексы полигонов с проверенным OBJ. Состав сохранён; максимальная разница координат после записи Blender — 9,50 × 10⁻⁷ м. Это точность переноса имеющейся сетки, а не точность исходных CAD-поверхностей.

Кривые, текст и точки не экспортированы. Поэтому результат — визуальный вывод всех Brep, а не перенос всех 491 объектов и не точная NURBS-версия. Проверка не подтверждает полноту проектной документации, готовность интерьера, замкнутость корпуса, натурное соответствие или пригодность для производства. Камера, материалы и окончательное изображение не входят в эту численную проверку.

Доказательства и точные SHA: [исходник и OBJ](review-check.json), [независимое чтение Blender](review-blender.json). Воспроизведение: `.local/cad-tools/rhino/venv/bin/python deliverables/V02/v001/01_support/review_check.py`, затем `blender --background --python deliverables/V02/v001/01_support/review_blender.py`. Скрипты записывают только собственные проверочные JSON.
