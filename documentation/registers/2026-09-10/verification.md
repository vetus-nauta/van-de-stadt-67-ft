# Проверка поставки 10.09.2026

Область: приёмка, устранение точных дублей, классификация и сохранность. Исходная версия репозитория — `acdbd0f98ac2ce4ce98ce887bacf972c1541af08`.

- PASS: пять переданных файлов доступны; исходные ZIP проходят CRC-проверку всех вложений.
- PASS: 11 вхождений сведены к 7 уникальным файлам (6 технических, 1 служебный). SHA-256 и размеры рабочих копий совпадают с входными байтами.
- PASS: `2-1.3dm` повторён трижды, `7-1.pdf` и `8-1.pdf` — дважды каждый. Сэкономлены 4 повторных экземпляра; оригинальные поставки сохранены локально.
- PASS: PDF открываются и рендерятся, по одному листу A0/A1; оба просмотрены. PDF не имеют извлекаемого текстового слоя, размеры не извлекались автоматически.
- PASS: оба JPEG декодируются и просмотрены; маленький эскиз 248 × 67 пикселей сохранён отдельно от размерных чертежей.
- PASS: служебный JSON разобран, содержит только реакцию почты и версию формата.
- PARTIAL: DWG имеет сигнатуру AC1018, 3DM — заголовок Rhino 3. Профильные CAD-программы в текущем окружении не обнаружены; полноценное открытие, слои, единицы, поверхности, внешние ссылки и геометрическая целостность не проверены.
- NOT_CHECKED: соответствие чертежей построенному корпусу и полнота комплекта для строительства. Принятие файлов не утверждает инженерные решения.

Повторяемая проверка реестров: `python3 tools/verify_documents.py` из корня репозитория. Синхронизация выпуска проверяется после commit/push по совпадению локального и удалённого SHA и дерева Git; подтверждение фиксируется при завершении работы. Контрольные суммы исходной поставки — в `delivery-manifest.json`.
